# Additive OpenLore governance CI. Safe to template-sync: unique filename.
# Advisory findings do not fail the job. Fail only if the CLI crashes.
#
# Prefer a repo-local .openlore/config.json (never template-synced). Fall back
# to the org starter at .github/openlore-config.json so analyze can run before
# a repo has run `openlore init`.
name: openlore-ci

on:
  pull_request:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: openlore-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  openlore:
    name: openlore-ci
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # openlore@3.2.0 requires Node ^22.19.0 || >=23.5.0 (math-desktop
          # EPI-18); the old '22.13' pin here predates that and is below the
          # CLI's engine floor. A bare '22' tracks the runner's latest 22.x
          # instead of going stale again.
          node-version: 22

      - name: Resolve OpenLore config
        id: openlore-config
        run: bash scripts/resolve-openlore-config.sh >> "$GITHUB_OUTPUT"

      - name: Analyze
        run: npx --yes openlore@3.2.0 analyze --no-embed --config "${{ steps.openlore-config.outputs.config }}"

      - name: Enforce (advisory)
        run: npx --yes openlore@3.2.0 enforce --config "${{ steps.openlore-config.outputs.config }}"
